# Claude agent instructions

Read [`AGENTS.md`](AGENTS.md) and then [`agents/AGENTS.md`](agents/AGENTS.md) before changing this repository.

The current production code under `src/` is authoritative. Documentation under `agents/` must mirror it. Use `agents/TICKETS.md` as the only active backlog and update the agent documentation whenever code changes make it stale.
